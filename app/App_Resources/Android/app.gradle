// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "net.richardbondi.clightningclient"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
